---
- name: Wait for instance to fully boot
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Pause for 20 seconds
      ansible.builtin.pause:
        seconds: 20

- import_playbook: playbooks/install_syslog.yml
- import_playbook: playbooks/configure_syslog_forwarding.yml
